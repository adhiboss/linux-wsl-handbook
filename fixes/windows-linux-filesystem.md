# Windows vs Linux Filesystem in WSL

## Where Linux Files are Stored
Your Linux home directory is inside:

Example:

## Access Windows Drives in WSL
Windows drives are mounted inside:

Examples:
- C drive → `/mnt/c`
- D drive → `/mnt/d`

Example:
```bash
cd /mnt/c/Users/Adithya/Desktop
/home/adhi/projects/
