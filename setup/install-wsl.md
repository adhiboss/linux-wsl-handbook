# WSL Installation Guide

## What is WSL
Windows Subsystem for Linux (WSL) allows running a Linux environment directly on Windows without virtual machines.

## Requirements
- Windows 10 (2004+) or Windows 11
- Administrator access

## Installation (Windows PowerShell – Admin)
```powershell
wsl --install
